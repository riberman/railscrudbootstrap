sudo: required

language: ruby

services:
  - docker


before_install:
  - sudo apt-get update
  - docker-compose build
  - docker-compose -f docker-compose.commands.yml run --rm website rails db:create db:migrate db:test:prepare

script:
 - docker-compose -f docker-compose.commands.yml run
